"""Dataset emitters: turn reconstructed chat rows into Tinker JSONL or HF Parquet.

Imported lazily by the build CLI so a format's optional backend dependency is
only required when that format is selected.
"""
